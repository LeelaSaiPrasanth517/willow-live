export function reconcileObservations(observations=[]){if(!observations.length)return{winner:null,agreement:0,provisional:true};const groups=new Map();for(const o of observations){const k=JSON.stringify({status:o.status,home:o.home_score,away:o.away_score,innings:o.innings,overs:o.overs});const a=groups.get(k)||[];a.push(o);groups.set(k,a);}const ranked=[...groups.values()].sort((a,b)=>b.length-a.length||(b[0].weight??0)-(a[0].weight??0)),winner=ranked[0][0],total=observations.reduce((s,o)=>s+(o.weight??0),0)||1,win=ranked[0].reduce((s,o)=>s+(o.weight??0),0);return{winner,agreement:Math.min(1,win/total),provisional:ranked[0].length===1&&observations.length>1};}
