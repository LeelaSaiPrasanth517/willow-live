export class EventDeduper{constructor(max=5000){this.max=max;this.seen=new Map();}key(e){return [e.event_id,e.source,e.source_event_id??"",e.match_id,e.innings??"",e.over??"",e.ball??""].join("|");}has(e){return this.seen.has(this.key(e));}add(e){const k=this.key(e);if(this.seen.has(k))return false;this.seen.set(k,Date.now());while(this.seen.size>this.max)this.seen.delete(this.seen.keys().next().value);return true;}clear(){this.seen.clear();}}
